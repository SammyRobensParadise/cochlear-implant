function [interval_audio_matrix] = filter_bandpass(audio_matrix,min_freq_range,max_freq_range)
%FILTER_BANDPASS returns an audio matrix of the specified sub_interval
interval_audio_matrix = audio_matrix;
end

